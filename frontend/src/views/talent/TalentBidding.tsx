'use client';

// Bidding is no longer a standalone page — it lives as a tab inside
// Subscriptions and Assignments (BiddingListView + TalentOffersView).
// The /talent/bidding route redirects to /talent/subscriptions?tab=bidding.

export default function TalentBidding() {
  return null;
}
