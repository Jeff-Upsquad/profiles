<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Where the in-app updater downloads APKs before install. getTemporaryDirectory()
         (path_provider) maps to the app cache dir on Android. -->
    <cache-path name="apk_cache" path="." />
    <external-cache-path name="ext_apk_cache" path="." />
    <files-path name="files" path="." />
</paths>
