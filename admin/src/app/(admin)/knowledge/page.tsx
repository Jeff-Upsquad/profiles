'use client';

import KnowledgeCenter from '@/views/knowledge/KnowledgeCenter';

/**
 * Knowledge Center — what Squad Bot knows (synced from SquadHub Resources).
 * Shares the `training` permission slug, like Webinars.
 */
export default function KnowledgePage() {
  return <KnowledgeCenter />;
}
